---
import { services } from "../utils/services";
import { projects } from "../utils/projects";
import { prices } from "../utils/prices";
import { process } from "../utils/process";
---

<!doctype html>
<html lang="pl">
	<head>
		<meta charset="UTF-8" />
		<meta
			name="description"
			content="Niki codes - profesjonalne tworzenie stron internetowych"
		/>

		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Niki codes</title>

		<style>
			.mobile-menu {
				transform: translateX(100%);
				transition: transform 0.3s ease-in-out;
			}

			.mobile-menu.active {
				transform: translateX(0);
			}

			.burger-line {
				transition: all 0.3s ease-in-out;
				height: 4px;
				width: 40px;
				background-color: #e5e7eb;
				border-radius: 5px;
			}

			.burger-active .line1 {
				transform: rotate(45deg) translate(8.5px, 8.5px);
			}

			.burger-active .line2 {
				opacity: 0;
			}

			.burger-active .line3 {
				transform: rotate(-45deg) translate(8.5px, -8.5px);
			}
		</style>
	</head>
	<body class="bg-gray-50 text-gray-900">
		<header
			class="min-h-screen relative overflow-hidden bg-gradient-to-br from-primary to-secondary"
		>
			<nav
				class="fixed top-0 left-0 right-0 bg-customWhite/10 backdrop-blur-lg border-b border-gray/20 z-50"
			>
				<div class="container mx-auto px-4 bg-secondary text-gray-50">
					<div class="flex items-center justify-between h-16">
						<a href="/" class="text-md font-bold">Niki Codes</a>
						<div class="hidden md:flex space-x-8">
							<a
								href="#uslugi"
								class="text-customWhite hover:text-blue-900 transition sm:text-gray-900 sm:font-semibold"
								>Usługi</a
							>
							<a
								href="#portfolio"
								class="text-customWhite hover:text-blue-900 transition sm:text-gray-900 sm:font-semibold"
								>Portfolio</a
							>
							<a
								href="#kontakt"
								class="text-customWhite hover:text-blue-900 transition sm:text-gray-900 sm:font-semibold"
								>Kontakt</a
							>
						</div>

						<button
							id="burger-btn"
							class="md:hidden flex flex-col justify-center items-center gap-y-2 z-50"
							aria-label="Menu"
						>
							<span class="burger-line line1"></span>
							<span class="burger-line line2"></span>
							<span class="burger-line line3"></span>
						</button>
					</div>
				</div>
				<div
					id="mobile-menu"
					class="mobile-menu fixed top-0 right-0 h-screen w-full bg-secondary backdrop-blur-lg bg-opacity-98 shadow-lg z-20 md:hidden"
				>
					<div class="flex flex-col pt-20 px-6">
						<a
							href="#uslugi"
							class="text-customWhite text-center py-3 border-b border-customWhite/10 hover:pl-2 transition-all duration-300 font-bold"
							>Usługi</a
						>
						<a
							href="#portfolio"
							class="text-customWhite text-center py-3 border-b border-customWhite/10 hover:pl-2 transition-all duration-300 font-bold"
							>Portfolio</a
						>
						<a
							href="#kontakt"
							class="text-customWhite text-center py-3 border-b border-customWhite/10 hover:pl-2 transition-all duration-300 font-bold"
							>Kontakt</a
						>
					</div>
				</div>
			</nav>
			<div
				class="relative z-10 container mx-auto h-screen flex items-center justify-center"
			>
				<div class="text-center max-w-4xl mx-auto">
					<h1 class="text-4xl md:text-6xl font-bold mb-6">
						<span class="block text-white">Twoja wizja.</span>
						<span class="text-2xl block mt-2 text-white"
							>Nasze rozwiązanie.</span
						>
						<span class="block mt-2 text-cyan-200"> Twoja nowa strona. </span>
					</h1>

					<div
						class="flex flex-col sm:flex-row gap-4 justify-center max-w-64 sm:max-w-none mx-8"
					>
						<a
							href="#portfolio"
							class="px-8 py-4 bg-customWhite/10 backdrop-blur-lg text-customWhite border border-customWhite/20 rounded-full font-semibold hover:bg-customWhite/20 hover:scale-105 transition-all duration-300"
						>
							Zobacz portfolio
						</a>
						<a
							href="#kontakt"
							class="px-8 py-4 bg-customWhite text-primary rounded-full font-semibold hover:bg-blue-50 transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl"
						>
							Rozpocznij projekt
						</a>
					</div>
				</div>
			</div>
		</header>
		<main id="main-content">
			<section class="py-20">
				<div class="container mx-auto px-4">
					<h2 class="text-4xl font-bold text-center mb-4">Nasze usługi</h2>
					<p class="text-gray-600 text-center mb-16 max-w-2xl mx-auto">
						Dostarczamy kompleksowe rozwiązania dopasowane do Twoich potrzeb
					</p>
					<div class="grid grid-cols-1 md:grid-cols-3 gap-8">
						{
							services.map((service) => (
								<article class="group relative overflow-hidden rounded-2xl bg-gradient-to-br from-primary/5 to-secondary/5 p-1">
									<div class="absolute inset-0 bg-gradient-to-br from-primary to-secondary opacity-0 group-hover:opacity-10 transition-opacity duration-300" />
									<div class="relative bg-customWhite p-8 rounded-xl h-full flex flex-col">
										<div class="mb-6">
											<span class="inline-flex items-center justify-center w-16 h-16 rounded-xl bg-primary/5 text-primary">
												<img
													src={service.icon}
													alt={service.alt}
													class="w-8 h-8"
												/>
											</span>
										</div>
										<h3 class="text-xl font-semibold mb-4">{service.title}</h3>
										<p class="text-gray-600">{service.desc}</p>
									</div>
								</article>
							))
						}
					</div>
				</div>
			</section>
			<section class="py-20 bg-gray-50">
				<div class="container mx-auto px-4">
					<h2 class="text-4xl font-bold text-center mb-4">Nasze realizacje</h2>
					<p class="text-gray-600 text-center mb-16 max-w-2xl mx-auto">
						Zobacz przykłady naszych najnowszych projektów
					</p>
					<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
						{
							projects.map((project) => (
								<article class="group relative cursor-pointer">
									<div class="sm:hidden text-center pb-4">
										<h3 class="font-bold">{project.title}</h3>
										<div>{project.desc}</div>
									</div>
									<div>
										<a href={project.link} target="_blank">
											<img
												src={project.img}
												alt={project.title}
												class="w-full h-full object-cover sm:transition-transform sm:duration-500"
											/>
										</a>
									</div>
									<div class="hidden sm:block absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-0 group-hover:opacity-100 transition-all duration-300">
										<div class="absolute bottom-0 left-0 right-0 p-4 text-customWhite transform translate-y-6 group-hover:translate-y-0 transition-transform duration-300">
											<h3 class="text-xl font-semibold mb-2">
												{project.title}
											</h3>
											<p class="text-gray-200 mb-4">{project.desc}</p>
										</div>
									</div>
								</article>
							))
						}
					</div>
				</div>
			</section>

			<section class="py-20 bg-primary">
				<div class="container mx-auto px-4">
					<h2 class="text-4xl font-bold text-center mb-4 text-customWhite">
						Oferta
					</h2>
					<div class="grid grid-cols-1 md:grid-cols-3 gap-8 bg-primary/5">
						{
							prices.map((price) => (
								<article class="group relative overflow-hidden rounded-2xl bg-gradient-to-br from-primary/5 to-secondary/5 p-1">
									<div class="absolute inset-0 bg-gradient-to-br from-primary to-secondary opacity-0 group-hover:opacity-10 transition-opacity duration-300" />
									<div class="relative bg-customWhite py-8 px-4 rounded-xl h-full flex flex-col">
										<h3 class="text-xl text-center font-semibold mb-4">
											{price.title}
										</h3>
										{price.points ? (
											<ul class="list-disc">
												{price.points.map((el) => (
													<li class="text-gray-600 ml-5 mb-2">{el}</li>
												))}
											</ul>
										) : (
											<div class="text-gray-600 text-center">{price.desc}</div>
										)}
										<div class="flex justify-center gap-1 mt-4 text-sm font-bold py-4 bg-secondary text-customWhite border border-customWhite/20 rounded-full">
											<div>Cena:</div>
											<div>{price.price}</div>
										</div>
									</div>
								</article>
							))
						}
					</div>
				</div>
			</section>

			<section class="py-20 bg-gray-50">
				<div class="container mx-auto px-4">
					<h2 class="text-4xl font-bold text-center mb-4">Jak działamy?</h2>
					<div class="relative mt-10">
						<div class="absolute left-8 top-0 bottom-0 w-0.5 bg-yellow/40">
						</div>

						{
							process.map((el) => (
								<div class="group mb-16">
									<div class="flex items-center gap-8 relative">
										<div
											class={`w-16 h-16 rounded-full flex items-center justify-center text-white shadow-lg group-hover:scale-110 transition-transform ${el.alert ? "bg-purple" : "bg-yellow"}`}
										>
											<span>
												<img
													width="40"
													height="auto"
													src={el.src}
													alt={el.alt}
												/>
											</span>
										</div>
										<div class="bg-white p-6 rounded-xl shadow-lg flex-1 group-hover:-translate-y-1 transition-transform shadow-yellow/10">
											<h3 class="text-gray-600">{el.title}</h3>
										</div>
									</div>
								</div>
							))
						}

						<!-- <div class="group mb-16">
							<div class="flex items-center gap-8 relative">
								<div
									class="w-16 h-16 rounded-full bg-blue-600 flex items-center justify-center text-white shadow-lg group-hover:scale-110 transition-transform"
								>
									<span class="text-2xl">🎨</span>
								</div>
								<div
									class="bg-white p-6 rounded-xl shadow-lg flex-1 group-hover:-translate-y-1 transition-transform"
								>
									<h3 class="text-xl font-bold mb-2">Prototyp</h3>
									<p class="text-gray-600">Design i makiety</p>
								</div>
							</div>
						</div> -->

						<!-- <div class="group mb-16">
							<div class="flex items-center gap-8 relative">
								<div
									class="w-16 h-16 rounded-full bg-blue-600 flex items-center justify-center text-white shadow-lg group-hover:scale-110 transition-transform"
								>
									<span class="text-2xl">💻</span>
								</div>
								<div
									class="bg-white p-6 rounded-xl shadow-lg flex-1 group-hover:-translate-y-1 transition-transform"
								>
									<h3 class="text-xl font-bold mb-2">Programowanie</h3>
									<p class="text-gray-600">Implementacja</p>
								</div>
							</div>
						</div> -->

						<!-- <div class="group mb-16">
							<div class="flex items-center gap-8 relative">
								<div
									class="w-16 h-16 rounded-full bg-blue-600 flex items-center justify-center text-white shadow-lg group-hover:scale-110 transition-transform"
								>
									<span class="text-2xl">✓</span>
								</div>
								<div
									class="bg-white p-6 rounded-xl shadow-lg flex-1 group-hover:-translate-y-1 transition-transform"
								>
									<h3 class="text-xl font-bold mb-2">Ocena</h3>
									<p class="text-gray-600">Testy i poprawki</p>
								</div>
							</div>
						</div> -->

						<!-- <div class="group mb-16">
							<div class="flex items-center gap-8 relative">
								<div
									class="w-16 h-16 rounded-full bg-blue-600 flex items-center justify-center text-white shadow-lg group-hover:scale-110 transition-transform"
								>
									<span class="text-2xl">📦</span>
								</div>
								<div
									class="bg-white p-6 rounded-xl shadow-lg flex-1 group-hover:-translate-y-1 transition-transform"
								>
									<h3 class="text-xl font-bold mb-2">Oddanie</h3>
									<p class="text-gray-600">Wdrożenie</p>
								</div>
							</div>
						</div> -->
					</div>
				</div>
			</section>

			<footer class="bg-gray-900 text-customWhite py-12" role="contentinfo">
				<div class="container mx-auto px-4">
					<div class="flex flex-col md:flex-row justify-between items-center">
						<div class="mb-8 md:mb-0">
							<div class="text-2xl font-bold mb-4">Natalia Nikiforuk</div>
							<p class="text-gray-400">Profesjonalne strony internetowe</p>
							<address class="text-gray-400 mt-4 not-italic">
								<p>
									Tel: <a
										href="tel:+48512227842"
										class="hover:text-customWhite transition">+48 512 227 842</a
									>
								</p>
								<p>
									Email: <a
										href="mailto:kontakt@webmaster.pl"
										class="hover:text-customWhite transition"
										>kontakt@webmaster.pl</a
									>
								</p>
							</address>
						</div>
					</div>
					<div
						class="border-t border-gray-800 mt-8 pt-8 text-center text-gray-400"
					>
						<p>&copy; {new Date().getFullYear()} Niki codes</p>
					</div>
				</div>
			</footer>
			<script>
				const burgerBtn = document.getElementById(
					"burger-btn"
				) as HTMLButtonElement | null;
				const mobileMenu = document.getElementById(
					"mobile-menu"
				) as HTMLDivElement | null;

				if (burgerBtn && mobileMenu) {
					burgerBtn.addEventListener("click", () => {
						burgerBtn.classList.toggle("burger-active");
						mobileMenu.classList.toggle("active");

						const isExpanded = mobileMenu.classList.contains("active");
						burgerBtn.setAttribute("aria-expanded", String(isExpanded));
					});

					const mobileLinks =
						mobileMenu.querySelectorAll<HTMLAnchorElement>("a");
					mobileLinks.forEach((link) => {
						link.addEventListener("click", () => {
							mobileMenu.classList.remove("active");
							burgerBtn.classList.remove("burger-active");
							burgerBtn.setAttribute("aria-expanded", "false");
						});
					});

					document.addEventListener("click", (e: MouseEvent) => {
						const target = e.target as HTMLElement;
						if (
							target &&
							!mobileMenu.contains(target) &&
							!burgerBtn.contains(target) &&
							mobileMenu.classList.contains("active")
						) {
							mobileMenu.classList.remove("active");
							burgerBtn.classList.remove("burger-active");
							burgerBtn.setAttribute("aria-expanded", "false");
						}
					});
				}
			</script>
		</main>
	</body>
</html>
